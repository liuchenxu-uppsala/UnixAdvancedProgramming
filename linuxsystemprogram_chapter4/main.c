#include <stdio.h>#include <sys/uio.h>#include <unistd.h>#include <fcntl.h>#include <string.h>int main(void) {    printf("Hello, World!\n");    char* buffer[3] = {        "The term buccaneer comes from...\n",        "A boucan is a wooden frame...\n",        "Buccaneer is the West Indies name...\n"    };    struct iovec iovecs[3] ={};    for (int i = 0; i < 3; i++) {        iovecs[i].iov_base = buffer[i];        iovecs[i].iov_len = strlen(buffer[i]);    }    int fd = open("file.txt", O_RDWR | O_CREAT | O_TRUNC, 0644);    writev(fd, iovecs, 3);    close(fd);    return 0;}